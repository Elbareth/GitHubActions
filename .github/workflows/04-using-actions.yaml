name: 04 – Workflow uses Actions
on: 
  #push:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses:  actions/checkout@v3 
      - name: Printing Folders
        run: ls -la         
               