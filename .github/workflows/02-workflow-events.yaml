name: 02 – Workflow Events
on: 
  push:
  pull_request:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
        echo "Event name: ${{ github.event_name }}"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed step
        run: | 
            echo I will fail 
            exit 0  
      - name: Say goodbye
        run: echo Goodbye!  